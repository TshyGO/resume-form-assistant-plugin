# D06 Native Messaging 帧层与 health 回路 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `resume-pro-desktop --nm-host` 能通过 Native Messaging 的 stdio 帧协议收发消息，并对 `health` 返回一条 D05 规范响应。

**Architecture:** 新增纯库 crate `nm-frame` 负责 4 字节长度前缀的帧编解码，只认字节不认 JSON。`src-tauri/src/nm.rs` 把帧层接到 D05 的 `validate_request_bytes` 上并组织回复。`--nm-host` 走 `run()` 的早退分支，不启动 Tauri。

**Tech Stack:** Rust 2021、`resume-pro-protocol`（D05）、`serde_json`。无新增第三方依赖。

**Spec:** [docs/superpowers/specs/2026-09-08-d06-nm-frame-design.md](../specs/2026-09-08-d06-nm-frame-design.md)

---

## File Structure

| 文件 | 职责 |
| --- | --- |
| `desktop/crates/nm-frame/Cargo.toml` | 新 crate 清单，零第三方依赖 |
| `desktop/crates/nm-frame/src/lib.rs` | `read_frame` / `write_frame` / `FrameError` / `MAX_FRAME_BYTES`，含单测 |
| `desktop/src-tauri/src/nm.rs` | 帧层到 D05 校验的接线与应答组织，含单测 |
| `desktop/src-tauri/src/cli.rs` | 增加 `--nm-host` 参数与帮助文本 |
| `desktop/src-tauri/src/lib.rs` | `run()` 中 `--nm-host` 早退分支，声明 `mod nm` |
| `desktop/src-tauri/Cargo.toml` | 增加 `nm-frame` 与 `resume-pro-protocol` 依赖 |
| `desktop/src-tauri/tests/nm_host.rs` | 进程级测试：拉起真实二进制灌帧，断言 stdout 纯净 |
| `.github/workflows/desktop.yml` | 增加 nm-frame 测试步骤、NM 进程测试步骤、Rust 缓存条目 |

`nm-frame` 不依赖 `resume-pro-protocol`：帧层的上限数值与协议一致，但依赖方向保持单向，帧层可脱离协议单测。上限在 `nm.rs` 中用一条断言与 D05 对齐。

---

### Task 1: 创建 nm-frame crate 与读帧的正常路径

**Files:**
- Create: `desktop/crates/nm-frame/Cargo.toml`
- Create: `desktop/crates/nm-frame/src/lib.rs`

- [ ] **Step 1: 创建 Cargo.toml**

```toml
[package]
name = "nm-frame"
version = "0.1.0"
edition = "2021"
rust-version = "1.77.2"
description = "Native Messaging stdio framing for Resume Pro. Bytes only; it does not parse JSON."
publish = false

[dependencies]
```

- [ ] **Step 2: 写会失败的测试**

创建 `desktop/crates/nm-frame/src/lib.rs`，内容仅为测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn reads_a_frame_written_with_a_native_endian_prefix() {
        let body = br#"{"messageType":"health"}"#;
        let mut wire = Vec::new();
        wire.extend_from_slice(&(body.len() as u32).to_ne_bytes());
        wire.extend_from_slice(body);

        let mut cursor = std::io::Cursor::new(wire);
        let frame = read_frame(&mut cursor).unwrap();
        assert_eq!(frame.as_deref(), Some(&body[..]));
    }

    #[test]
    fn a_zero_length_frame_is_an_empty_body_not_an_end_of_stream() {
        let mut cursor = std::io::Cursor::new(0u32.to_ne_bytes().to_vec());
        assert_eq!(read_frame(&mut cursor).unwrap(), Some(Vec::new()));
    }
}
```

- [ ] **Step 3: 运行测试确认失败**

在 `desktop/` 下运行：`cargo test --manifest-path crates/nm-frame/Cargo.toml`
预期：编译失败，`cannot find function read_frame in this scope`

- [ ] **Step 4: 写最小实现**

在 `lib.rs` 的 `#[cfg(test)] mod tests` 之前插入：

```rust
//! Native Messaging stdio framing.
//!
//! Chrome frames each message as a 4-byte native-endian length prefix followed by
//! UTF-8 JSON. This crate moves bytes only; it never parses the body.

use std::io::Read;

/// Matches `resume_pro_protocol::MAX_ENVELOPE_BYTES`. A larger frame would be refused by
/// the contract layer anyway, so refusing it here avoids the allocation entirely.
pub const MAX_FRAME_BYTES: usize = 65536;

#[derive(Debug)]
pub enum FrameError {
    Io(std::io::Error),
    /// The length prefix claimed more than `MAX_FRAME_BYTES`. Nothing was allocated and
    /// the body was not read; the stream can no longer be trusted.
    TooLarge { declared: u32 },
    /// The stream ended part-way through a prefix or a body. A length-prefixed stream
    /// cannot resynchronise, so the caller must close the connection.
    Truncated,
}

impl From<std::io::Error> for FrameError {
    fn from(value: std::io::Error) -> Self {
        FrameError::Io(value)
    }
}

/// Read one frame. `Ok(None)` means the peer closed the port cleanly, which is a normal
/// end of session rather than an error.
pub fn read_frame<R: Read>(reader: &mut R) -> Result<Option<Vec<u8>>, FrameError> {
    let mut prefix = [0u8; 4];
    let read = fill(reader, &mut prefix)?;
    if read == 0 {
        return Ok(None);
    }
    if read < prefix.len() {
        return Err(FrameError::Truncated);
    }
    let declared = u32::from_ne_bytes(prefix);
    if declared as usize > MAX_FRAME_BYTES {
        return Err(FrameError::TooLarge { declared });
    }
    let mut body = vec![0u8; declared as usize];
    let read = fill(reader, &mut body)?;
    if read < body.len() {
        return Err(FrameError::Truncated);
    }
    Ok(Some(body))
}

/// Read until `buf` is full or the stream ends. Returns how many bytes were read.
fn fill<R: Read>(reader: &mut R, buf: &mut [u8]) -> std::io::Result<usize> {
    let mut filled = 0;
    while filled < buf.len() {
        match reader.read(&mut buf[filled..]) {
            Ok(0) => break,
            Ok(n) => filled += n,
            Err(e) if e.kind() == std::io::ErrorKind::Interrupted => continue,
            Err(e) => return Err(e),
        }
    }
    Ok(filled)
}
```

- [ ] **Step 5: 运行测试确认通过**

运行：`cargo test --manifest-path crates/nm-frame/Cargo.toml`
预期：`test result: ok. 2 passed`

- [ ] **Step 6: 提交（含新生成的 Cargo.lock）**

```bash
git add desktop/crates/nm-frame
git commit -m "feat(d06): add the Native Messaging frame reader

Chrome frames each message as a 4-byte native-endian length prefix
followed by UTF-8 JSON. This crate moves bytes only and never parses the
body, so the framing can be tested without the protocol crate.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 读帧的三条硬规则

**Files:**
- Modify: `desktop/crates/nm-frame/src/lib.rs`（仅新增测试）

- [ ] **Step 1: 写会失败的测试**

在 `mod tests` 内追加：

```rust
    #[test]
    fn a_closed_port_is_a_clean_end_of_session() {
        let mut cursor = std::io::Cursor::new(Vec::new());
        assert_eq!(read_frame(&mut cursor).unwrap(), None);
    }

    #[test]
    fn a_partial_prefix_cannot_resynchronise() {
        let mut cursor = std::io::Cursor::new(vec![1u8, 0, 0]);
        assert!(matches!(
            read_frame(&mut cursor),
            Err(FrameError::Truncated)
        ));
    }

    #[test]
    fn a_body_shorter_than_its_prefix_is_truncated() {
        let mut wire = 10u32.to_ne_bytes().to_vec();
        wire.extend_from_slice(b"only4");
        let mut cursor = std::io::Cursor::new(wire);
        assert!(matches!(
            read_frame(&mut cursor),
            Err(FrameError::Truncated)
        ));
    }

    #[test]
    fn an_oversized_prefix_is_refused_without_reading_or_allocating_the_body() {
        // The prefix claims 4 GiB. Nothing may be allocated, and the body bytes must
        // still be unread so the refusal is provably about the prefix alone.
        let mut wire = u32::MAX.to_ne_bytes().to_vec();
        wire.extend_from_slice(b"body-must-stay-unread");
        let mut cursor = std::io::Cursor::new(wire);
        assert!(matches!(
            read_frame(&mut cursor),
            Err(FrameError::TooLarge { declared: u32::MAX })
        ));
        assert_eq!(cursor.position(), 4);
    }

    #[test]
    fn the_largest_accepted_frame_is_the_protocol_envelope_limit() {
        let body = vec![b'x'; MAX_FRAME_BYTES];
        let mut wire = (body.len() as u32).to_ne_bytes().to_vec();
        wire.extend_from_slice(&body);
        let mut cursor = std::io::Cursor::new(wire);
        assert_eq!(read_frame(&mut cursor).unwrap(), Some(body));

        let mut over = ((MAX_FRAME_BYTES + 1) as u32).to_ne_bytes().to_vec();
        over.extend_from_slice(&vec![b'x'; MAX_FRAME_BYTES + 1]);
        let mut cursor = std::io::Cursor::new(over);
        assert!(matches!(
            read_frame(&mut cursor),
            Err(FrameError::TooLarge { .. })
        ));
    }
```

- [ ] **Step 2: 运行测试确认通过**

运行：`cargo test --manifest-path crates/nm-frame/Cargo.toml`
预期：`test result: ok. 7 passed`

这些测试对着 Task 1 的实现应当直接通过。若 `an_oversized_prefix...` 的 `cursor.position()` 断言失败，说明实现读了正文，必须修实现而不是改断言。

- [ ] **Step 3: 提交**

```bash
git add desktop/crates/nm-frame/src/lib.rs
git commit -m "test(d06): pin the three hard rules of frame reading

A clean end of stream is a normal end of session, a partial frame cannot
resynchronise and must close the connection, and an oversized length
prefix is refused before anything is read or allocated. The last one
asserts the cursor advanced only past the prefix, so the refusal is
provably about the prefix rather than a later failure.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: 写帧

**Files:**
- Modify: `desktop/crates/nm-frame/src/lib.rs`

- [ ] **Step 1: 写会失败的测试**

在 `mod tests` 内追加：

```rust
    #[test]
    fn writes_a_frame_that_reads_back_identically() {
        let body = br#"{"ok":true}"#;
        let mut wire = Vec::new();
        write_frame(&mut wire, body).unwrap();
        assert_eq!(wire.len(), 4 + body.len());

        let mut cursor = std::io::Cursor::new(wire);
        assert_eq!(read_frame(&mut cursor).unwrap().as_deref(), Some(&body[..]));
    }

    #[test]
    fn refuses_to_write_beyond_the_envelope_limit() {
        let body = vec![b'x'; MAX_FRAME_BYTES + 1];
        let mut wire = Vec::new();
        assert!(matches!(
            write_frame(&mut wire, &body),
            Err(FrameError::TooLarge { .. })
        ));
        assert!(wire.is_empty(), "nothing may be written for a refused frame");
    }
```

- [ ] **Step 2: 运行测试确认失败**

运行：`cargo test --manifest-path crates/nm-frame/Cargo.toml`
预期：编译失败，`cannot find function write_frame in this scope`

- [ ] **Step 3: 写最小实现**

把 `use std::io::Read;` 改为：

```rust
use std::io::{Read, Write};
```

在 `fill` 函数之前插入：

```rust
/// Write one frame. Refuses an oversized body without writing anything, so a caller's
/// mistake cannot leave a half-written frame that desynchronises the stream.
pub fn write_frame<W: Write>(writer: &mut W, body: &[u8]) -> Result<(), FrameError> {
    if body.len() > MAX_FRAME_BYTES {
        return Err(FrameError::TooLarge {
            declared: body.len() as u32,
        });
    }
    writer.write_all(&(body.len() as u32).to_ne_bytes())?;
    writer.write_all(body)?;
    writer.flush()?;
    Ok(())
}
```

- [ ] **Step 4: 运行测试确认通过**

运行：`cargo test --manifest-path crates/nm-frame/Cargo.toml`
预期：`test result: ok. 9 passed`

- [ ] **Step 5: 提交**

```bash
git add desktop/crates/nm-frame/src/lib.rs
git commit -m "feat(d06): add the frame writer

Refuses an oversized body before writing anything. A half-written frame
would desynchronise the stream permanently, which is worse than the
error the caller already has to handle.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 把 nm-frame 接入 CI

**Files:**
- Modify: `.github/workflows/desktop.yml`

- [ ] **Step 1: 增加缓存 workspace**

找到 `Cache Rust` 步骤的 `workspaces:` 列表，在 `desktop/crates/protocol` 之后追加一行：

```yaml
            desktop/crates/nm-frame
```

- [ ] **Step 2: 增加测试步骤**

在 `- name: Protocol JS validator tests` 步骤之后、`- name: Set up Python` 之前插入：

```yaml
      - name: Frame layer tests
        working-directory: desktop
        run: cargo test --manifest-path crates/nm-frame/Cargo.toml --locked --target ${{ matrix.rust-target }}
```

- [ ] **Step 3: 本地按 CI 形式验证**

在 `desktop/` 下运行：`cargo test --manifest-path crates/nm-frame/Cargo.toml --locked --target x86_64-pc-windows-msvc`
预期：`test result: ok. 9 passed`

若报 `the lock file needs to be updated`，先运行一次不带 `--locked` 的 `cargo test` 生成锁文件并一并提交。

- [ ] **Step 4: 校验 YAML 可解析**

在仓库根运行：

```bash
python -c "import yaml;d=yaml.safe_load(open('.github/workflows/desktop.yml',encoding='utf-8'));print([s['name'] for s in d['jobs']['desktop']['steps']])"
```

预期：步骤列表中出现 `Frame layer tests`

- [ ] **Step 5: 提交**

```bash
git add .github/workflows/desktop.yml desktop/crates/nm-frame
git commit -m "ci(d06): run the frame layer on both platforms

D05 shipped without CI coverage and its contract tests ran only on
developer machines until that was noticed. Wiring the new crate in at
creation time rather than afterwards.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: 增加 --nm-host 参数

**Files:**
- Modify: `desktop/src-tauri/src/cli.rs`

- [ ] **Step 1: 写会失败的测试**

把 `cli.rs` 末尾的 `#[cfg(test)] mod tests` 替换为：

```rust
#[cfg(test)]
mod tests {
    #[test]
    fn help_text_does_not_claim_reminders() {
        let text = include_str!("cli.rs");
        assert!(text.contains("does not register Native Messaging"));
        assert!(text.contains("reminders"));
    }

    #[test]
    fn nm_host_never_attaches_a_console() {
        // Attaching a console puts a stream on stdout, and a Native Messaging host whose
        // stdout carries anything but protocol frames is broken in a way that surfaces
        // only as an unexplained disconnect in the browser.
        let text = include_str!("cli.rs");
        let start = text
            .find("pub fn prepare_stdio")
            .expect("prepare_stdio must exist");
        let body = &text[start..];
        let end = body.find("\n}").expect("prepare_stdio must be a function");
        assert!(
            !body[..end].contains("--nm-host"),
            "prepare_stdio must not attach a console for --nm-host"
        );
    }

    #[test]
    fn help_lists_the_native_messaging_flag() {
        let text = include_str!("cli.rs");
        assert!(text.contains("--nm-host"));
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

在 `desktop/` 下运行：`cargo test --manifest-path src-tauri/Cargo.toml --lib nm_host`
预期：`help_lists_the_native_messaging_flag` 失败，`assertion failed: text.contains("--nm-host")`

- [ ] **Step 3: 写最小实现**

在 `cli.rs` 中，`Args` 结构追加字段：

```rust
pub struct Args {
    pub probe: bool,
    pub hidden: bool,
    pub quit: bool,
    pub help: bool,
    pub apps_loop: bool,
    pub nm_host: bool,
}
```

`parse()` 中追加变量与分支，并在返回值里带上：

```rust
pub fn parse() -> Args {
    let mut probe = false;
    let mut hidden = false;
    let mut quit = false;
    let mut help = false;
    let mut apps_loop = false;
    let mut nm_host = false;
    for arg in std::env::args().skip(1) {
        match arg.as_str() {
            "--probe" => probe = true,
            "--hidden" => hidden = true,
            "--quit" => quit = true,
            "--apps-loop" => apps_loop = true,
            "--nm-host" => nm_host = true,
            "--help" | "-h" => help = true,
            _ => {}
        }
    }
    Args {
        probe,
        hidden,
        quit,
        help,
        apps_loop,
        nm_host,
    }
}
```

`print_help()` 的 Usage 行与选项列表更新为：

```
Usage:
  resume-pro-desktop [--hidden] [--probe] [--apps-loop] [--nm-host] [--quit] [--help]

  --probe      Print host status JSON and exit. Does not remain as the unique writer.
  --hidden     Start the unique-writer host without showing the main window.
  --apps-loop  Run the D04 application-manager persistence loop on an isolated directory and exit.
  --nm-host    Speak the Native Messaging stdio protocol on stdin/stdout and exit when the port closes.
  --quit       Ask the existing unique-writer process to exit. Do not start a second host.
  --help       Show this message.
```

`prepare_stdio()` 不改动。它按名单附加控制台，`--nm-host` 不在名单内，因此不会附加。

- [ ] **Step 4: 运行测试确认通过**

运行：`cargo test --manifest-path src-tauri/Cargo.toml --lib`
预期：全部通过，含新增三项

- [ ] **Step 5: 提交**

```bash
git add desktop/src-tauri/src/cli.rs
git commit -m "feat(d06): add the --nm-host flag

prepare_stdio attaches a console by an explicit list of flags, and
--nm-host is deliberately absent from it: a console puts a stream on
stdout, and a Native Messaging host whose stdout carries anything but
protocol frames fails as an unexplained disconnect in the browser. A
test asserts the flag stays out of that list.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: 应答组织

**Files:**
- Create: `desktop/src-tauri/src/nm.rs`
- Modify: `desktop/src-tauri/Cargo.toml`
- Modify: `desktop/src-tauri/src/lib.rs:1-5`

- [ ] **Step 1: 增加依赖**

`desktop/src-tauri/Cargo.toml` 的 `[dependencies]` 追加两行：

```toml
nm-frame = { path = "../crates/nm-frame" }
resume-pro-protocol = { path = "../crates/protocol" }
```

`desktop/src-tauri/src/lib.rs` 顶部的模块声明追加：

```rust
mod nm;
```

- [ ] **Step 2: 写会失败的测试**

创建 `desktop/src-tauri/src/nm.rs`，内容仅为测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use serde_json::Value;

    const HEALTH: &str = r#"{"protocolVersion":1,"messageId":"33333333-3333-4333-8333-333333333333","clientInstanceId":"11111111-1111-4111-8111-111111111111","messageType":"health","occurredAt":"2026-09-06T12:00:00.000Z","payload":{}}"#;

    fn respond(request: &str) -> Value {
        let raw = response_for(request.as_bytes()).expect("a response is expected");
        serde_json::from_slice(&raw).expect("the response must be JSON")
    }

    #[test]
    fn health_gets_a_successful_response_correlated_to_the_request() {
        let response = respond(HEALTH);
        assert_eq!(response["ok"], true);
        assert_eq!(response["protocolVersion"], 1);
        assert_eq!(response["correlationId"], "33333333-3333-4333-8333-333333333333");
        assert_eq!(response["payload"], serde_json::json!({}));
    }

    #[test]
    fn a_request_this_slice_cannot_serve_is_reported_unavailable_not_successful() {
        // job.save is a valid write. Nothing behind this slice can persist it, so it must
        // say so rather than claim success.
        let save = HEALTH
            .replace("\"messageType\":\"health\"", "\"messageType\":\"job.save\"")
            .replace(
                "\"payload\":{}",
                "\"payload\":{\"sourceRestoreEpoch\":\"bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb\",\"company\":\"合成公司\",\"title\":\"后端实习\",\"payloadSha256\":\"1ea8fcf15e56dd83a5e7f8e9adb0c34b94bc28fd5c1b51400ecf597d1f5cc8c4\"}",
            )
            .replace(
                "\"payload\":",
                "\"archiveId\":\"aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa\",\"restoreEpoch\":\"bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb\",\"payload\":",
            );
        let response = respond(&save);
        assert_eq!(response["ok"], false);
        assert_eq!(response["error"]["code"], "unavailable");
        assert_eq!(response["error"]["retryable"], true);
        assert!(response.get("resultId").is_none());
    }

    #[test]
    fn a_rejected_request_is_answered_with_its_own_message_id() {
        let bad = HEALTH.replace("\"protocolVersion\":1", "\"protocolVersion\":9");
        let response = respond(&bad);
        assert_eq!(response["ok"], false);
        assert_eq!(response["correlationId"], "33333333-3333-4333-8333-333333333333");
        assert_eq!(response["error"]["code"], "protocol_incompatible");
        assert_eq!(response["error"]["retryable"], false);
    }

    #[test]
    fn an_error_message_never_echoes_the_payload_back() {
        // Validator messages quote the offending value. Echoing one would hand rejected
        // content back to the extension, so responses carry a fixed message per code.
        let bad = HEALTH.replace(
            "\"occurredAt\":\"2026-09-06T12:00:00.000Z\"",
            "\"occurredAt\":\"2026-99-99T99:99:99Z\"",
        );
        let response = respond(&bad);
        assert_eq!(response["ok"], false);
        let message = response["error"]["message"].as_str().unwrap_or_default();
        assert!(!message.contains("2026-99-99"), "message echoed the input: {message}");
        assert!(message.len() <= 300);
    }

    #[test]
    fn a_frame_without_a_usable_message_id_gets_no_response() {
        assert!(response_for(b"not json at all").is_none());
        assert!(response_for(br#"{"messageId":"not-a-uuid"}"#).is_none());
    }
}
```

- [ ] **Step 3: 运行测试确认失败**

在 `desktop/` 下运行：`cargo test --manifest-path src-tauri/Cargo.toml --lib nm::`
预期：编译失败，`cannot find function response_for in this scope`

- [ ] **Step 4: 写最小实现**

在 `nm.rs` 的 `#[cfg(test)] mod tests` 之前插入：

```rust
//! Native Messaging wiring: frames in, D05-validated responses out.
//!
//! This slice does not reach the archive. Anything it cannot serve is answered
//! `unavailable`, which is the one retryable code in D05, rather than a false success.

use resume_pro_protocol::{validate_request_bytes, ErrorCode, MessageType, MAX_ENVELOPE_BYTES};
use serde_json::{json, Value};

const _FRAME_LIMIT_MATCHES_ENVELOPE: () = assert!(nm_frame::MAX_FRAME_BYTES == MAX_ENVELOPE_BYTES);

/// Build the response for one received frame.
///
/// `None` means no compliant response can be built, because the D05 response envelope
/// requires a `correlationId` and this frame carries no usable `messageId`. The caller
/// closes the connection instead of emitting something the extension would also reject.
pub fn response_for(frame: &[u8]) -> Option<Vec<u8>> {
    match validate_request_bytes(frame) {
        Ok(request) => {
            let response = if request.message_type == MessageType::Health {
                json!({
                    "protocolVersion": 1,
                    "correlationId": request.message_id,
                    "ok": true,
                    "payload": {}
                })
            } else {
                error_response(&request.message_id, ErrorCode::Unavailable)
            };
            serde_json::to_vec(&response).ok()
        }
        Err(err) => {
            let message_id = message_id_of(frame)?;
            serde_json::to_vec(&error_response(&message_id, err.code)).ok()
        }
    }
}

/// A fixed message per code. Validator messages quote the offending value, so forwarding
/// one would hand rejected content back to the extension.
fn error_response(correlation_id: &str, code: ErrorCode) -> Value {
    json!({
        "protocolVersion": 1,
        "correlationId": correlation_id,
        "ok": false,
        "payload": {},
        "error": {
            "code": code.as_str(),
            "retryable": code == ErrorCode::Unavailable,
            "message": fixed_message(code)
        }
    })
}

fn fixed_message(code: ErrorCode) -> &'static str {
    match code {
        ErrorCode::Unavailable => "The desktop archive service is not connected in this build.",
        ErrorCode::ProtocolIncompatible => "The request protocol version is not supported.",
        ErrorCode::UnknownMessageType => "The request message type is not supported.",
        ErrorCode::PayloadTooLarge => "The request exceeds the envelope limit.",
        ErrorCode::SecretForbidden => "The request carries content that must not be stored.",
        _ => "The request was rejected by contract validation.",
    }
}

/// The top-level `messageId`, only when it is a syntactically valid UUID. Anything else
/// cannot correlate a response.
fn message_id_of(frame: &[u8]) -> Option<String> {
    let value: Value = serde_json::from_slice(frame).ok()?;
    let id = value.get("messageId")?.as_str()?;
    let shape = [8usize, 4, 4, 4, 12];
    let mut groups = id.split('-');
    for len in shape {
        let group = groups.next()?;
        if group.len() != len || !group.chars().all(|c| c.is_ascii_hexdigit()) {
            return None;
        }
    }
    if groups.next().is_some() {
        return None;
    }
    Some(id.to_string())
}
```

- [ ] **Step 5: 运行测试确认通过**

运行：`cargo test --manifest-path src-tauri/Cargo.toml --lib nm::`
预期：`test result: ok. 5 passed`

依赖的外部签名已核实（`53ea509`）：`ErrorCode` 派生 `Copy + PartialEq`，`pub fn as_str(self) -> &'static str`（按值取 self，故 `code.as_str()` 与 `code == ErrorCode::Unavailable` 均成立）；变体名为 `Unavailable`、`ProtocolIncompatible`、`UnknownMessageType`、`PayloadTooLarge`、`SecretForbidden`；`Request` 的字段为 `message_id: String` 与 `message_type: MessageType`；`MessageType::Health` 存在；`MAX_ENVELOPE_BYTES` 经 `pub use types::*` 导出。

- [ ] **Step 6: 提交**

```bash
git add desktop/src-tauri/src/nm.rs desktop/src-tauri/Cargo.toml desktop/src-tauri/src/lib.rs
git commit -m "feat(d06): answer a validated frame with a D05 response

health succeeds. Anything else this slice cannot serve is answered
unavailable, the one retryable code in D05, rather than a false success;
that branch is temporary and goes away when the archive service is wired
in.

Error responses carry a fixed message per code rather than the
validator's own text, which quotes the offending value and would hand
rejected content back to the extension. A frame with no usable messageId
gets no response at all, because the response envelope requires a
correlationId and an invalid response would be rejected by the extension
too, hiding the real cause.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: 服务循环与 run() 接线

**Files:**
- Modify: `desktop/src-tauri/src/nm.rs`
- Modify: `desktop/src-tauri/src/lib.rs:457-462`

- [ ] **Step 1: 写会失败的测试**

在 `nm.rs` 的 `mod tests` 内追加：

```rust
    fn framed(body: &str) -> Vec<u8> {
        let mut wire = (body.len() as u32).to_ne_bytes().to_vec();
        wire.extend_from_slice(body.as_bytes());
        wire
    }

    #[test]
    fn a_closed_port_ends_the_session_with_success() {
        let mut input = std::io::Cursor::new(Vec::new());
        let mut output = Vec::new();
        assert_eq!(serve(&mut input, &mut output), 0);
        assert!(output.is_empty());
    }

    #[test]
    fn two_health_frames_get_two_responses_and_nothing_else_on_stdout() {
        let mut wire = framed(HEALTH);
        wire.extend_from_slice(&framed(HEALTH));
        let mut input = std::io::Cursor::new(wire);
        let mut output = Vec::new();
        assert_eq!(serve(&mut input, &mut output), 0);

        let mut cursor = std::io::Cursor::new(output);
        for _ in 0..2 {
            let frame = nm_frame::read_frame(&mut cursor)
                .expect("a frame is expected")
                .expect("the stream must not end early");
            let value: Value = serde_json::from_slice(&frame).unwrap();
            assert_eq!(value["ok"], true);
        }
        assert_eq!(
            nm_frame::read_frame(&mut cursor).unwrap(),
            None,
            "stdout must carry protocol frames and nothing else"
        );
    }

    #[test]
    fn an_oversized_prefix_closes_the_session_without_answering() {
        let mut wire = u32::MAX.to_ne_bytes().to_vec();
        wire.extend_from_slice(b"body");
        let mut input = std::io::Cursor::new(wire);
        let mut output = Vec::new();
        assert_eq!(serve(&mut input, &mut output), 2);
        assert!(output.is_empty());
    }
```

- [ ] **Step 2: 运行测试确认失败**

运行：`cargo test --manifest-path src-tauri/Cargo.toml --lib nm::`
预期：编译失败，`cannot find function serve in this scope`

- [ ] **Step 3: 写最小实现**

在 `nm.rs` 顶部的 `use` 之后插入：

```rust
use std::io::{Read, Write};

/// Read frames until the port closes. Returns the process exit code.
///
/// Diagnostics go to stderr only. Anything written to stdout other than a protocol frame
/// breaks the channel, and the browser reports it as an unexplained disconnect.
pub fn serve<R: Read, W: Write>(input: &mut R, output: &mut W) -> i32 {
    loop {
        match nm_frame::read_frame(input) {
            Ok(None) => return 0,
            Ok(Some(frame)) => {
                let Some(response) = response_for(&frame) else {
                    eprintln!("nm-host: frame carries no usable messageId; closing");
                    return 2;
                };
                if let Err(err) = nm_frame::write_frame(output, &response) {
                    eprintln!("nm-host: cannot write response: {err:?}");
                    return 2;
                }
            }
            Err(err) => {
                eprintln!("nm-host: cannot read frame: {err:?}");
                return 2;
            }
        }
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

运行：`cargo test --manifest-path src-tauri/Cargo.toml --lib nm::`
预期：`test result: ok. 8 passed`

- [ ] **Step 5: 接到 run()**

在 `lib.rs` 的 `run()` 中，`if args.help { ... }` 分支之后、`if args.apps_loop` 之前插入：

```rust
    if args.nm_host {
        let mut input = std::io::stdin();
        let mut output = std::io::stdout();
        std::process::exit(nm::serve(&mut input, &mut output));
    }
```

放在 `apps_loop` 之前是因为 `--nm-host` 绝不能落进任何会向 stdout 打印的分支。

- [ ] **Step 6: 运行全部单测**

运行：`cargo test --manifest-path src-tauri/Cargo.toml --bins --lib`
预期：全部通过

- [ ] **Step 7: 提交**

```bash
git add desktop/src-tauri/src/nm.rs desktop/src-tauri/src/lib.rs
git commit -m "feat(d06): serve frames until the port closes

The dispatch sits before every branch that prints, so --nm-host can
never fall into a path that puts non-protocol bytes on stdout.
Diagnostics go to stderr only.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: 进程级测试与 CI 步骤

**Files:**
- Create: `desktop/src-tauri/tests/nm_host.rs`
- Modify: `.github/workflows/desktop.yml`

- [ ] **Step 1: 写会失败的测试**

创建 `desktop/src-tauri/tests/nm_host.rs`：

```rust
//! Drives the real binary over stdio, the way a browser would. No registration, no
//! browser, no system state.

use std::io::{Read, Write};
use std::process::{Command, Stdio};

const HEALTH: &str = r#"{"protocolVersion":1,"messageId":"33333333-3333-4333-8333-333333333333","clientInstanceId":"11111111-1111-4111-8111-111111111111","messageType":"health","occurredAt":"2026-09-06T12:00:00.000Z","payload":{}}"#;

fn framed(body: &str) -> Vec<u8> {
    let mut wire = (body.len() as u32).to_ne_bytes().to_vec();
    wire.extend_from_slice(body.as_bytes());
    wire
}

/// Send `input` to a fresh `--nm-host` process and return (exit code, stdout, stderr).
fn run_host(input: Vec<u8>) -> (i32, Vec<u8>, String) {
    let mut child = Command::new(env!("CARGO_BIN_EXE_resume-pro-desktop"))
        .arg("--nm-host")
        .stdin(Stdio::piped())
        .stdout(Stdio::piped())
        .stderr(Stdio::piped())
        .spawn()
        .expect("the binary must start");
    child
        .stdin
        .take()
        .expect("stdin is piped")
        .write_all(&input)
        .expect("the host must accept input");
    let output = child.wait_with_output().expect("the host must exit");
    let mut stderr = String::new();
    output.stderr.as_slice().read_to_string(&mut stderr).ok();
    (output.status.code().unwrap_or(-1), output.stdout, stderr)
}

#[test]
fn a_health_frame_is_answered_and_stdout_carries_nothing_else() {
    let (code, stdout, _stderr) = run_host(framed(HEALTH));
    assert_eq!(code, 0);

    let mut expected = Vec::new();
    let response = br#"{"protocolVersion":1,"correlationId":"33333333-3333-4333-8333-333333333333","ok":true,"payload":{}}"#;
    expected.extend_from_slice(&(response.len() as u32).to_ne_bytes());
    expected.extend_from_slice(response);
    assert_eq!(
        stdout, expected,
        "stdout must be exactly one protocol frame, byte for byte"
    );
}

#[test]
fn a_closed_port_exits_cleanly_and_prints_nothing() {
    let (code, stdout, stderr) = run_host(Vec::new());
    assert_eq!(code, 0);
    assert!(stdout.is_empty());
    assert!(stderr.is_empty(), "a clean close is not an error: {stderr}");
}

#[test]
fn an_oversized_prefix_is_refused_on_stderr_and_stdout_stays_empty() {
    let mut wire = u32::MAX.to_ne_bytes().to_vec();
    wire.extend_from_slice(b"body");
    let (code, stdout, stderr) = run_host(wire);
    assert_eq!(code, 2);
    assert!(stdout.is_empty(), "a refusal must not put bytes on stdout");
    assert!(stderr.contains("nm-host"), "the reason must reach stderr: {stderr}");
}
```

断言中的响应字节序列依赖 `serde_json` 保持插入顺序，`response_for` 构造 `json!` 时的键序即为 `protocolVersion`、`correlationId`、`ok`、`payload`。若顺序不符导致断言失败，改为解析后逐字段比较，不要放宽“stdout 只有一帧”的断言。

- [ ] **Step 2: 运行测试确认失败**

在 `desktop/` 下运行：`cargo test --manifest-path src-tauri/Cargo.toml --test nm_host`
预期：全部三项失败（`--nm-host` 尚未接线时为超时或非零退出）。若 Task 7 已完成，本步骤应直接通过，此时在提交信息中如实写明测试是后补的回归护栏。

- [ ] **Step 3: 运行测试确认通过**

运行：`cargo test --manifest-path src-tauri/Cargo.toml --test nm_host`
预期：`test result: ok. 3 passed`

- [ ] **Step 4: 增加 CI 步骤**

在 `.github/workflows/desktop.yml` 的 `- name: App tests` 步骤之后插入：

```yaml
      - name: NM host process test
        working-directory: desktop
        run: cargo test --manifest-path src-tauri/Cargo.toml --locked --test nm_host --target ${{ matrix.rust-target }}
```

现有 `App tests` 步骤用 `--bins --lib`，不包含 `tests/` 下的集成测试，因此必须单列一步，否则本测试不会在 CI 运行。

- [ ] **Step 5: 校验 YAML 并按 CI 形式本地跑**

```bash
python -c "import yaml;d=yaml.safe_load(open('.github/workflows/desktop.yml',encoding='utf-8'));print([s['name'] for s in d['jobs']['desktop']['steps']])"
```

预期：出现 `NM host process test`

在 `desktop/` 下运行：`cargo test --manifest-path src-tauri/Cargo.toml --locked --test nm_host --target x86_64-pc-windows-msvc`
预期：`test result: ok. 3 passed`

- [ ] **Step 6: 提交**

```bash
git add desktop/src-tauri/tests/nm_host.rs .github/workflows/desktop.yml
git commit -m "test(d06): drive the real binary over stdio

Runs the shipped executable the way a browser would, with no
registration and no system state. The health test compares stdout byte
for byte rather than parsing it, because the property that matters is
that stdout carries protocol frames and nothing else.

App tests run with --bins --lib, which excludes tests/, so this needs
its own CI step or it would never run there.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 9: 全量回归与开 PR

**Files:** 无改动

- [ ] **Step 1: 按 CI 形式跑全套**

在 `desktop/` 下依次运行，全部应通过：

```bash
cargo test --manifest-path crates/nm-frame/Cargo.toml --locked --target x86_64-pc-windows-msvc
cargo test --manifest-path crates/protocol/Cargo.toml --locked --target x86_64-pc-windows-msvc
cargo test --manifest-path crates/data-service/Cargo.toml --locked --target x86_64-pc-windows-msvc
cargo test --manifest-path crates/archive-store/Cargo.toml --locked --target x86_64-pc-windows-msvc
cargo test --manifest-path src-tauri/Cargo.toml --locked --bins --lib --target x86_64-pc-windows-msvc
cargo test --manifest-path src-tauri/Cargo.toml --locked --test nm_host --target x86_64-pc-windows-msvc
node --test crates/protocol/js/*.test.mjs
node --test src/pairing-form.test.js src/applications.test.js src/applications-ui.test.js
```

在仓库根运行：`node --test tests/*.test.js`
预期：`pass 115`

- [ ] **Step 2: 空白检查**

```bash
git diff --check
```

预期：无输出

- [ ] **Step 3: 推送并开 PR**

```bash
git push -u origin codex/d06-nm-frame
```

PR 正文须写明：本片是 D06 的第一个小 PR；`unavailable` 是本片的临时行为，不是最终语义；未实现握手、未连接应用进程、未做任何注册；引用 spec 路径与 issue #24。

- [ ] **Step 4: 等 CI 双平台通过后再请求评审**

---

## Self-Review

**Spec 覆盖：** spec 第 2 节结构由 Task 1、6、7 实现；第 3 节三条硬规则由 Task 2 逐条钉死；第 4 节 stdout 纯净由 Task 5 的 `nm_host_never_attaches_a_console` 与 Task 8 的逐字节断言双重覆盖；第 5 节四行应答语义由 Task 6 的五个测试覆盖；第 6 节测试要求由 Task 2、7、8 覆盖，CI 接入由 Task 4、8 覆盖；第 7 节「明确不做」体现为未出现任何数据库、进程拉起或注册相关任务。

**类型一致性：** `read_frame` 返回 `Result<Option<Vec<u8>>, FrameError>`，Task 2、3、7、8 的调用均按此签名；`write_frame(writer, body)` 参数顺序在 Task 3、7 一致；`response_for(&[u8]) -> Option<Vec<u8>>` 在 Task 6 定义、Task 7 调用一致；`serve(&mut R, &mut W) -> i32` 在 Task 7 定义并接入 `run()`。

**外部签名：** 已在 `53ea509` 上逐项核实并写入 Task 6，计划中不存在待确认的签名。
