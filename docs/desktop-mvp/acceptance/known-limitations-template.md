# 首发已知非阻断限制（模板）

> 发布前复制到本次 `runs/<run-id>/known-limitations.md`。这里只登记经过批准的非阻断限制；数据损坏、隐私泄漏、必测环境无法安装连接、未确认状态变化、恢复破坏旧库或发行字节不一致不能降级为“已知问题”。

| ID | 影响范围与用户可见表现 | 为什么不阻断 | 可行规避方法 | 负责人 | 后续 issue | 批准人/日期 |
| --- | --- | --- | --- | --- | --- | --- |
| 示例（发布前删除） | 特定 OS 通知能力受限 | 已按支持矩阵批准降级且核心档案/待办不受影响 | 应用内查看待办 | `@owner` | `#000` | `name / YYYY-MM-DD` |

若没有已知非阻断限制，正式文件明确写“本次发行无已批准的非阻断限制”，不要保留空表或示例行。
