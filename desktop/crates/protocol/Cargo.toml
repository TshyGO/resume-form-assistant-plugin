[package]
name = "resume-pro-protocol"
version = "0.1.0"
edition = "2021"
rust-version = "1.77.2"
description = "Resume Pro D05 plugin–desktop JSON contract. Not a Native Messaging host."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"


